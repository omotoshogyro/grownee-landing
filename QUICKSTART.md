# Quick Start Guide

Get your Grownee landing page up and running in minutes!

## 🚀 Quick Installation

1. **Extract the files** (if downloaded as zip)

2. **Install dependencies:**
   ```bash
   cd grownee-landing
   npm install
   ```

3. **Start development server:**
   ```bash
   npm run dev
   ```

4. **Open in browser:**
   Navigate to [http://localhost:3000](http://localhost:3000)

That's it! Your landing page should now be running.

## 📦 What's Included

- ✅ Fully functional landing page
- ✅ Responsive design (mobile, tablet, desktop)
- ✅ Smooth animations
- ✅ All components ready to use
- ✅ TypeScript configured
- ✅ Tailwind CSS setup

## 🛠️ Common Commands

```bash
# Development
npm run dev          # Start dev server

# Production
npm run build        # Build for production
npm start            # Start production server

# Code Quality
npm run lint         # Run ESLint
```

## 📝 Making Changes

### Update Colors
Edit `tailwind.config.ts` to change the color scheme

### Update Content
Modify component files in the `components/` folder

### Add New Sections
Create new component files and import them in `app/page.tsx`

## ❓ Need Help?

Check the full README.md for detailed documentation.

## 🎨 Customization Tips

1. **Brand Colors:** Change primary color in `tailwind.config.ts`
2. **Fonts:** Update Google Fonts import in `app/globals.css`
3. **Logo:** Replace the "G" text with your logo in `Header.tsx`
4. **Content:** Update text in each component file

## 🌐 Deploy

Easy deployment options:
- **Vercel:** Import from GitHub (recommended)
- **Netlify:** Deploy from Git
- **Others:** Any Node.js hosting platform

Happy coding! 🎉
